# Future Optional OpenCode Agent: Research Critic

This file is an example for a future integration. It is not used by the `research-xm` CLI.

Purpose:

- Review `report.md` against `evidence.json`.
- Flag unsupported claims.
- Suggest stronger source coverage and citation fixes.

The MVP critic is implemented as a TypeScript role function and Xiaomi chat call.
